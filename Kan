git init
git remote add origin <repository-url>
git add .
git commit -m "初次提交"
git push -u origin main

git checkout -b feature/my-feature（创建分支）

git checkout main
git merge feature/my-feature（合并分支）

git branch -d feature/my-feature（合并后删除分支）

name: CI/CD Pipeline

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Set up Node.js
        uses: actions/setup-node@v2
        with:
          node-version: '14'

      - name: Install dependencies
        run: npm install

      - name: Deploy
        run: npm run deploy

feat: 添加用户登录功能
fix: 修复注册页面的错误
docs: 更新 README 文件

# 变更日志

## [1.0.0] - 2024-10-09
### 添加
- 用户登录功能
- 注册页面

### 修复
- 修复了一些小错误
















































